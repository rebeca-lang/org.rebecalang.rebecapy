reactiveclass Queue(5) {       
	statevars { 
		stack	waiting;		
	} 
  
	Queue() { 
		int i;
		
        for( i=0; i<12; i++){
    		waiting.push(i);			
        }
		
		trace( waiting.count() );

        for( i=0; i<12; i++){
			trace( waiting.pop() );
        }
	}   
} 

main { 
	Queue q():(); 
} 