reactiveclass Stack(5) {       
	statevars { 
		stack	waiting;		
	} 
  
	Stack() { 
		int i;
		
        for( i=0; i<12; i++){
    		waiting.push(i);			
        }
		
		trace( waiting.count() );

        for( i=0; i<12; i++){
			trace( waiting.pop() );
        }
	}   
} 

main { 
	Stack q():(); 
} 